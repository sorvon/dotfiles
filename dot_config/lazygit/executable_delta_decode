#!/usr/bin/env bash

while IFS= read -r line; do
    if echo "$line" | iconv -f UTF-8 -t UTF-8 > /dev/null 2>&1; then
        echo "$line"
    else
        echo "$line" | iconv -f GBK -t UTF-8 2>/dev/null || echo "$line"
    fi
done | delta --paging=never
