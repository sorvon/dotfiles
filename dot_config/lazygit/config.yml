git:
  diffRenderers:
    - command: $HOME/.config/lazygit/delta_decode
      colorArg: always
    - command: delta --paging=never
      colorArg: always
keybinding:
  universal:
    suspendApp: <disabled>
os:
  editPreset: "nvim-remote"
  copyToClipboardCmd: $HOME/.config/lazygit/osc52 {{text}}
